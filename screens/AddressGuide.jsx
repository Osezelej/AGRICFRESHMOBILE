import {View, StyleSheet, FlatList, Text} from 'react-native';
import { memo } from 'react';

const styles = StyleSheet.create({

})

function AddresseGuide(){
    return <View style={styles.body}>
        
    </View>
}

export default memo(AddresseGuide)